# Agent rules

Phase 0 documents are authoritative. Map the architecture before modification; do not add duplicate components, schemas, clients or future packages. Keep changes scoped, never write production data, and never use destructive commands without explicit approval. Every change needs fresh validation evidence. Do not push, deploy or create a PR unless explicitly requested. Final reports list files, commands, tests, risks and rollback.
