# Frontend rules

Use Next.js App Router only. Do not create generic dashboards, business pricing constants, authentication UI or booking UI in Phase 1. Server authorization remains authoritative. Introduce design tokens before business UI, include accessible form states when forms exist, respect reduced motion and verify visible flows with Playwright. `packages/ui` appears only when reusable components have real consumers.
