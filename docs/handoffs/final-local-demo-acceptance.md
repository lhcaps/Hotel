# Final Local Demo Acceptance Report

Generated: 2026-08-01
Branch: `main`
Author: see `git log -1 --format='%an <%ae>'` for the current HEAD
Archive: `output/room-management-demo.zip` (next to this archive: `output/room-management-demo.zip.sha256`)
SHA-256: see the external `output/room-management-demo.zip.sha256` file. The authoritative
integrity record is **outside** the archive; `verification/SHA256-EXPECTED.txt`
inside the archive is advisory only and was generated by the packager
at build time.

> To verify the package, compute
> `Get-FileHash output\room-management-demo.zip -Algorithm SHA256`
> and compare to the line in
> `output\room-management-demo.zip.sha256`.

## Platform support

The demo orchestration tooling (`RUN-DEMO.ps1`, `VERIFY-DEMO.ps1`,
`STOP-DEMO.ps1`, `scripts/demo/start-local.mjs`,
`scripts/demo/stop.mjs`) is **Windows-only**. It uses `tasklist`,
`taskkill /PID`, and `Get-NetTCPConnection` and refuses to run on
macOS / Linux. The Web / API / worker source remains portable, but
the customer package's orchestration is Windows-only.

## 1. Closure commit on `main`

```
$(git log -1 --format='%h %s')
$(git rev-parse HEAD)
```

Resolve with `git rev-parse HEAD` and `git log -1 --pretty=oneline` on
your checkout.

## 2. Quality gates

All gates ran on the canonical `localhost` origins:

- `http://localhost:3000` (Web)
- `http://localhost:3001` (API)
- `http://localhost:3090` (Payment simulator)
- `http://localhost:8025` (Mailpit)

| Gate                         | Command                                        | Result           |
| ---------------------------- | ---------------------------------------------- | ---------------- |
| Prettier format check        | `npx prettier --check <touched files>`         | PASS             |
| ESLint (turbo)               | `npx turbo lint`                               | PASS (9/9)       |
| TypeScript typecheck (turbo) | `npx turbo typecheck`                          | PASS (9/9)       |
| OpenAPI drift check          | `npx tsx scripts/generate-openapi.mts --check` | PASS             |
| Database check               | `pnpm db:check` (drizzle-kit)                  | PASS             |
| Unit tests                   | `npx turbo test:unit --concurrency=1`          | PASS (337 tests) |
| Production build             | `npx turbo build`                              | PASS (9/9)       |
| Local demo verifier          | `node scripts/demo/verify.mjs`                 | PASS (16/16)     |

Full logs for each gate are bundled at `verification/INDEX.md` inside
the archive. See `verification/SHA256-EXPECTED.txt` for the archive's
SHA-256 (advisory — trust the external `.sha256` file instead).

## 3. Final local demo acceptance (Playwright)

`tests/e2e/final-local-demo-acceptance.spec.ts` — run with
`playwright.verify.config.ts` against the canonical `localhost` stack.

| Scenario | Description                                                      |
| -------- | ---------------------------------------------------------------- |
| A        | Public catalog renders on landing                                |
| B        | Unauthenticated CUSTOMER browser pages and API returns 401       |
| C        | MOMO full browser flow → `/booking/manage/{code}` auto-redirect  |
| D        | VNPAY full browser flow → `/booking/manage/{code}` auto-redirect |
| E        | Forged MoMo return cannot confirm a booking                      |
| F        | ADMIN login, `/api/admin/me` 200, protected admin pages render   |
| G        | ADMIN refresh persists session, logout revokes                   |

Result: **7/7 passed** against the closure commit, run multiple times.
Logs are at `verification/final-acceptance-run{1,2}.log`.

## 4. Demo stack topology

| Service           | Host                  | Port | Started by                         |
| ----------------- | --------------------- | ---- | ---------------------------------- |
| Web (Next.js)     | http://localhost:3000 | 3000 | `RUN-DEMO.ps1` → `start-local.mjs` |
| API (NestJS)      | http://localhost:3001 | 3001 | `RUN-DEMO.ps1` → `start-local.mjs` |
| Worker            | (no public port)      | —    | `RUN-DEMO.ps1` → `start-local.mjs` |
| Payment Simulator | http://localhost:3090 | 3090 | `RUN-DEMO.ps1` → `start-local.mjs` |
| Mailpit           | http://localhost:8025 | 8025 | `docker compose up -d mailpit`     |
| Postgres / Redis  | docker                | —    | `docker compose up -d`             |

`STOP-DEMO.ps1` reads `.demo/start-manifest.json` and terminates only
the PIDs the runner recorded. It never sweeps ports and never uses
`taskkill /IM`.

## 5. Package

- Created by `scripts/package-demo.mjs` from `git rev-parse HEAD`.
- Top-level directory inside the zip:
  `room-management-demo-<short-sha>/`.
- SHA-256 is written to
  `output/room-management-demo.zip.sha256` (external, authoritative).
  A copy is embedded at `verification/SHA256-EXPECTED.txt` for
  in-archive discoverability.

## 6. Cleanliness

```
$ git status --short
```

- Allowed: `D <ignored-path>` (a tracked file that is also matched by
  `.gitignore` and was removed by a previous exploratory run).
- Disallowed: any other modification, addition, or staged deletion.
- `output/room-management-demo.zip` and
  `output/room-management-demo.zip.sha256` are generated artefacts and
  are not committed.

## 7. Demo accounts

| Role          | Email                    | Password                   |
| ------------- | ------------------------ | -------------------------- |
| Administrator | `demo-verify@room.local` | `Aa1-KnownVerifyPass-1234` |

Customer flow uses the booking-hold OTP channel via Mailpit — no
account is required.

## 8. Reproduce the verification

```powershell
# from inside the extracted archive
docker compose up -d
pnpm install --frozen-lockfile
pnpm demo:db:create
pnpm demo:seed
.\RUN-DEMO.ps1
.\VERIFY-DEMO.ps1
npx playwright test tests/e2e/final-local-demo-acceptance.spec.ts `
    --config=playwright.verify.config.ts --workers=1 --retries=0
.\STOP-DEMO.ps1
docker compose down
```

The same commands work in PowerShell on Windows. This package does
not support macOS / Linux for the orchestration tooling.
